export default class Personne {
    constructor(nom, prenom, surnom, dateNaissance){
        this.nom = nom;
        this.prenom = prenom;
        this.surnom = surnom;
        this.dateNaissance = dateNaissance;
    }
}